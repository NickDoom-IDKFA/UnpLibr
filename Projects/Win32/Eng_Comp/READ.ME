This directory containg decompression engines for non-archive compression formats.
Single files are also processed here, but inner non-compressed archives (which are commonly found inside non-archive compressed files) require /Generic/* modules.